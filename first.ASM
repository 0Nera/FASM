use16   ; Говорим создавать 16-битный код
org 100h ; Говорим начинать программу с адреса 100h
mov ax,255 ; Присваеваем регистру ax 255(ax = 255)
inc ax ; Увеличиваем ax на 1 (ax = ax + 1 или ax++)
nop ; Эта инструкция ничего не делает, но мы будем использовать её для отладки. Она имеет HEX код 90
mov bx,ax ; Присваиваем регистру bx значение ax (bx = ax)
mov ax,4C00h ; Последние 2 строчки - конец программы
int 21h